export default class Project {
    constructor(name, color) {
        this.name = name;
        this.color = color;
    }

    setName(newName) {
        this.name = newName;
    }

    setColor(newColor) {
        this.color = newColor;
    }
}
